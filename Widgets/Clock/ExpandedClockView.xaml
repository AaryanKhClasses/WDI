<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WDI.Widgets.Clock.ExpandedClockView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WDI.Widgets.Clock"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.8*" />
            <ColumnDefinition Width="1.2*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Padding="0,0,20,0">
            <StackPanel VerticalAlignment="Center">
                <TextBlock
                    x:Name="ExpandedTimeText" Text="00:00"
                    FontSize="48" FontWeight="Bold" Foreground="White"
                />
                <TextBlock
                    x:Name="ExpandedDayText" Text="Sunday" Margin="0,8,0,0"
                    FontSize="18" Foreground="White" Opacity="0.85"
                />
                <TextBlock
                    x:Name="ExpandedDateText" Text="1 January 1970" Margin="0,4,0,0"
                    FontSize="15" Foreground="White" Opacity="0.65"
                />
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock
                x:Name="CalendarMonthText" Grid.Row="0"
                FontSize="18" FontWeight="SemiBold" Foreground="White"
                HorizontalAlignment="Center" Margin="0,0,0,12"
            />
            <Grid x:Name="CalendarGrid" Grid.Row="1" />
        </Grid>
    </Grid>
</UserControl>
